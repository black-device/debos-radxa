# Blackdevice recipe FTR 2023
  - action: run
    chroot: true
    script: scripts/blackdevice.sh

  - action: overlay
    origin: name
    source: custom
    destination: /usr/lib/linux-image-$(uname -r)/rockchip/overlay
  
  #- action: run
  #  description: Copy file
  #  chroot: false
  #  command: cp ${ROOTDIR}/list.txt ${ARTIFACTDIR}/list.txt

